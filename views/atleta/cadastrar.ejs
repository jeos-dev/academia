<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <div class="container">
        <h2><%= atleta._id ? "Atualizar" : "Cadastrar" %> Atleta</h2>
    <form action="<%= atleta._id ? "/atletas/editar" : "/atletas" %>" method="post">
        <input type="hidden" name="_id" value="<%= atleta._id %>">
        
    <div>
        <label class="form-label">Matrícula:</label>
        <input class="form-control" type="text" name="mat" value="<%= atleta.mat %>" required>
    </div>

    <div>
        <label class="form-label">Nome:</label>
        <input class="form-control" type="text" name="nome" value="<%= atleta.nome %>" required>
    </div>

    <div>
        <label class="form-label">Biotipo:</label>
        <input class="form-control" type="text" name="biotipo" value="<%= atleta.biotipo %>" required>
    </div><br>

    <div>
        <button type="submit" class="btn btn-success"> <%= atleta._id ? "Atualizar" : "Salvar" %></button>
        <a href="/" class="btn btn-primary">Voltar</a>
    </div>
</form>
</div>

    <%- include("../partials/rodape.ejs") %>
</body>
</html>