<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/cabecalho.ejs') %>
    <div class="container">
        <h2><%= usuario._id ? "Atualizar" : "Cadastrar" %> Usuário</h2>
    <form action="<%= usuario._id ? "/usuarios/editar" : "/usuarios" %>" method="post">
        <input type="hidden" name="_id" value="<%= usuario._id %>">
        
    <div>
        <label class="form-label">Email:</label>
        <input class="form-control" type="text" name="email" value="<%= usuario.email %>" required>
    </div>

    <div>
        <label class="form-label">Nome:</label>
        <input class="form-control" type="text" name="nome" value="<%= usuario.nome %>" required>
    </div>

    <div>
        <label class="form-label">Senha:</label>
        <input class="form-control" type="text" name="senha" value="<%= usuario.senha %>" required>
    </div><br>

    <div>
        <button type="submit" class="btn btn-success"> <%= usuario._id ? "Atualizar" : "Salvar" %></button>
        <a href="/" class="btn btn-primary">Voltar</a>
    </div>
</form>
</div>

    <%- include("../partials/rodape.ejs") %>
</body>
</html>